services:
  - type: web
    name: invoice-reimburse-web
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /login
    envVars:
      - key: DATABASE_URL
        value: file:./prisma/dev.db
      - key: DEMO_MODE
        value: "true"
