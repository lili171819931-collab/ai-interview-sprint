{
  "name": "Time Utility",
  "version": "1.0.0",
  "description": "返回当前时间、时区与常用时间格式",
  "category": "Productivity",
  "tags": ["time", "时间", "date", "时区"],
  "icon": "🕐",
  "execution_type": "local",
  "risk_level": "low",
  "permissions": ["read"],
  "input_schema": {
    "type": "object",
    "properties": {
      "format": { "type": "string", "enum": ["iso", "date", "datetime", "timestamp"], "description": "时间格式", "required": false }
    },
    "required": []
  },
  "ai_description": "返回当前本地时间、UTC 时间与常用格式，供工作流与 Agent 使用。",
  "use_cases": ["时间戳", "工作流输入", "报告生成"],
  "examples": ["现在几点"]
}
