# GitHub API 配置（Token 只允许出现在服务器端环境变量，绝不出现在前端/代码/仓库）
# 复制为 .env 后填入（或使用「GitHub Integration」页面保存到本地 data/github-token）
# 数据目录（Token/缓存/限流状态）。生产 standalone 部署必须指向真实项目 data/ 绝对路径；开发模式可留空
AIOSS_DATA_DIR=
GITHUB_TOKEN=
GITHUB_API_URL=https://api.github.com
GITHUB_API_TIMEOUT=15000
GITHUB_API_MAX_RETRIES=3
GITHUB_API_CACHE_TTL=3600
# 遇到 403/429 限流时最多等待多久再重试/返回（毫秒，默认 30000 = 30 秒；未认证时立即返回友好错误）
GITHUB_MAX_RATE_WAIT_MS=30000
