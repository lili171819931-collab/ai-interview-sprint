# 🎥 AI Video Recorder

> 本地 AI 录屏 / 多摄像头合成 / 智能剪辑工作室 —— Screenity 同类型应用（Web 版，数据完全本地）

一款功能完整的 **AI 视频录制器**：录屏 + 双摄像头 + 多源合成 + 视频引擎（裁剪 / 缩放 / 字幕 /
模板 / BGM / AI 剪辑），一键导出并发布到 **TikTok / YouTube / 小红书 / 抖音**。全部在浏览器本地完成，
无需注册、无使用限制、不上传任何数据。

```
              AI Video Recorder
                       │
        ┌──────────────┼──────────────┐
        ↓              ↓              ↓
      Screen         Camera 1       Camera 2
        │              │              │
        └──────────────┼──────────────┘
                       ↓
                Multi-source
                  Composition
                       ↓
              ┌────────┴────────┐
              ↓                 ↓
          Main Screen        PiP Camera
              │                 │
              └────────┬────────┘
                       ↓
                 Video Engine
                       ↓
        ┌──────────────┼──────────────┐
        ↓              ↓              ↓
      Crop           Zoom           Subtitle
        ↓              ↓              ↓
     Template        AI Edit        BGM
                       ↓
                  Final Video
                       ↓
              TikTok / YouTube
              小红书 / 抖音
```

## ✨ 功能清单

### 多源采集（Screen + Camera 1 + Camera 2）
- 🖥️ **屏幕 / 窗口 / 标签页**：调用系统屏幕选择器，支持 60fps，可捕获系统声音
- 🎥 **Camera 1 / Camera 2**：支持多摄像头设备选择、镜像、独立开关与实时预览
- 🎙️ **麦克风**：音量调节、回声消除 / 降噪
- 🔊 **系统声音**：随屏幕捕获，音量独立控制

### 多源合成（Main Screen + PiP Camera）
- 实时 Canvas 合成引擎：屏幕为主画面，双摄像头为画中画（PiP）
- 画中画**可拖动 / 可缩放**（录制前），圆角、边框、标签
- 支持自定义背景图片

### 视频引擎
- ✂️ **Crop 裁剪**：画布上直接拖拽裁剪框 + 手柄，支持 16:9 / 9:16 / 1:1 / 4:3 预设与数值微调
- 🔎 **Zoom 缩放**：1x–4x 平滑缩放，点击画布设定聚焦点，聚焦点预设，**点击自动聚焦缩放**
- 💬 **Subtitle 字幕**：多行字幕、字号 / 颜色 / 底色 / 位置自定义，**AI 语音识别实时字幕**（Chrome/Edge），可导出 SRT / WebVTT
- 🎨 **Template 模板**：YouTube 16:9 / 抖音·TikTok 9:16 / 小红书 3:4 / 网课 / 游戏 60fps / 播客 1:1
- 🎵 **BGM 背景乐**：内置 4 首 WebAudio 实时合成的背景音乐（Lo-Fi / 流行 / 钢琴 / 合成波），音量可调，**说话时自动闪避**
- ✨ **AI 剪辑**：能量分析 → 自动剪除静音 / 标记高光 → 生成剪辑方案 → 智能重渲染最终视频（可叠加字幕 + BGM）
- ✏️ **标注工具**（Screenity 招牌）：画笔 / 箭头 / 方框 / 圆圈 / 文字 / 敏感内容模糊，颜色线宽可调
- 网格辅助线、水印、时间戳、录制暂停 / 继续、快捷键（空格 / ⌘R）

### 🎛️ OBS 核心功能（场景 / 来源 / 混音器 / 转场 / 滤镜）
底部新增 **OBS 风格控制台**，三栏布局：
- 🎬 **场景（Scenes）**：新建 / 切换 / 复制 / 重命名 / 删除；每个场景保存整套工作区配置（模板 / 分屏 / 小窗 / 美颜 / 模糊 / 裁剪 / 缩放 / 字幕 / BGM / 来源可见性 / 文字图片来源）；**录制中切换场景同样生效**；自动持久化到 localStorage（刷新后恢复）
- 🎞️ **转场（Transitions）**：切换场景时可选 无 / 淡入淡出 / 擦除
- 📦 **来源（Sources）**：屏幕 / Camera 1 / Camera 2 / 字幕 / BGM 的眼睛开关（OBS 可见性），可**添加文字来源**（可编辑/删除）与**图片来源**（本地上传，叠加到画面）
- 🔊 **混音器（Mixer）**：麦克风 / 系统声音 / BGM / 主输出 4 通道，每通道实时**电平表** + 音量推子 + 静音，主输出总控
- 🎞️ **滤镜（Filters）**：摄像头颜色滤镜（原图 / 暖阳 / 冷调 / 黑白 / 复古），与美颜叠加

### 🧩 融合升级（OBS + OpenScreen + Cap + Recordly）
- 🪞 **小窗视频（PiP）**：位置 / 大小可拖动缩放，**5 种形状**（圆角 / 圆形 / 椭圆 / 方形 / 菱形），镜像开关 —— 融合 Cap / OpenScreen 的浮窗与形状能力
- 🖥️ **分屏模式**：`画中画` / `上下分屏`（页面在上、小窗在下）/ `左右分屏` / `双摄并排` / `圆形浮窗`，一键切换（录制中也生效）
- 💄 **简单美颜**：磨皮 / 美白 / 红润 / 清晰度四档实时调节（录制中也生效）
- 🌫️ **背景模糊优化（三种）**：`页面模糊`（小窗背后网页内容虚化）/ `人像柔焦`（中心清晰边缘虚化）/ **`人像抠图`（人像保持清晰、真实背景模糊，MediaPipe 本地分割，完全离线）**
- ⏱ **录制倒计时**：0 / 3 / 5 / 10 秒可选，倒计时期间不录制，倒数结束自动开始（带提示音）
- 👁 **OBS 风格来源开关**：屏幕 / Camera 1 / Camera 2 可见性录制中随时切换
- ✨ **点击特效 + 自动聚焦缩放**（OpenScreen / Recordly）：网页内点击记录扩散圆环，可自动平滑聚焦到点击处
- 📥 **导入本地视频**：把网页内录制 / 任意视频导入继续 AI 剪辑、导出与发布
- 🌐 **网页内直接录制（Chrome 扩展）**：见下节

### 🌐 网页内录制（浏览器扩展）
在**任意真实网页上直接操作**：悬浮工具栏（录制 / 暂停 / 停止 / 计时）+ 摄像头小窗都显示在目标网页中，
录制的是网页本身 —— 无需打开本页面。

```bash
# 1) 打开扩展页
chrome://extensions
# 2) 右上角开启「开发者模式」
# 3) 「加载已解压的扩展程序」→ 选择本项目的 extension/ 文件夹
# 4) 任意网页右上角出现 🎥 悬浮工具栏，点「● 录制」
```

扩展能力：录屏（当前标签页 / 窗口 / 整个屏幕）、**录制倒计时（3/5/10 秒）**、
摄像头小窗（拖动 / 缩放 / 5 种形状 / **分屏模式：上下 / 左右 / 圆形浮窗** / 美颜 /
**人像抠图：人像清晰 + 真实背景模糊**）、麦克风混音、字幕（手动 / Chrome AI 语音识别）、
网页内点击特效、自动聚焦缩放；录完自动下载，并一键跳回本页面继续 AI 剪辑 / 发布。
录制时只需盯着真实网页操作：悬浮工具栏与小窗都显示在目标网页上。

> 注：Google Chrome（品牌版）已禁用命令行 `--load-extension`，请按上面步骤手动加载。

### 导出与发布
- 📦 录制输出 MP4 / WebM（按浏览器能力自动选择），可下载
- 📋 **复制视频到剪贴板**，直接粘贴到平台上传框（Chrome/Edge）
- 🚀 一键打开 **TikTok / YouTube / 小红书 / 抖音** 上传页
- 💬 导出 SRT / WebVTT 字幕文件

## 🚀 快速开始

```bash
npm install
npm run dev
# 打开 http://127.0.0.1:3220
```

生产构建：

```bash
npm run build && npm run preview
```

## 🧪 功能测试

内置两套 Playwright 端到端测试（使用 Chrome 的假摄像头 / 假麦克风 + 注入假屏幕源）：

```bash
# 先启动 dev server
npm run dev

# 主应用全流程：连接源 → 引擎（裁剪/缩放/小窗美颜/字幕/模板/BGM）→ 录制 → 导出 → AI 剪辑 → 发布
BASE_URL=http://127.0.0.1:3220 node tests/e2e.mjs

# 网页内录制（扩展内容脚本）：悬浮工具栏 → 摄像头小窗（形状/美颜/模糊）→ 录制 → 下载
BASE_URL=http://127.0.0.1:3220 node tests/extension-e2e.mjs
```

> 提示：测试默认使用系统已安装的 Google Chrome（`channel: "chrome"`），无需下载 Playwright 浏览器。
> 品牌版 Chrome 无法命令行加载扩展，因此扩展 E2E 直接注入 `extension/content.js` 验证相同代码路径，
> 并单独校验 `manifest.json`。

## 🏗️ 架构

```
src/
├── engine/
│   ├── Studio.ts          # 中枢：源管理 / 录制状态机 / 事件
│   ├── compositor.ts      # Canvas 合成引擎（屏幕+PiP+裁剪+缩放+字幕+标注）
│   ├── audioEngine.ts     # WebAudio 混音（麦克风+系统声+BGM+闪避+能量分析）
│   ├── bgm.ts             # WebAudio 合成背景音乐
│   ├── aiEdit.ts          # AI 剪辑（能量分析 → 剪辑方案 → 智能重渲染）
│   ├── templates.ts       # 六大平台模板
│   └── export.ts          # 下载 / SRT/VTT / 剪贴板 / 平台跳转
├── components/
│   ├── SourcePanel.tsx    # 音视频源面板
│   ├── CanvasStage.tsx    # 画布 + 标注 + 裁剪框 + PiP 拖动
│   ├── EnginePanel.tsx    # 裁剪/缩放/字幕/模板/BGM/AI 剪辑
│   └── ExportDialog.tsx   # 导出 & 发布
└── App.tsx                # 布局与快捷键
```

技术栈：**React 19 + TypeScript + Vite**，无后端、无第三方运行时依赖（除 React）。

## ⚠️ 说明
- 需在**本地/HTTPS**环境运行（浏览器媒体权限要求安全上下文，`http://127.0.0.1` 满足）
- 录音 / 录屏需用户授权；`getDisplayMedia` 会弹出系统选择器
- AI 语音识别字幕依赖浏览器 Web Speech API（Chrome / Edge 最佳）
- AI 剪辑完全本地计算，无需 API Key
